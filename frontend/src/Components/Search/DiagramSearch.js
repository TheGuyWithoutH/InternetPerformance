/**
 * @file Diagram search component for the search data page and diagram selection
 * @author Ugo Balducci
 * @version 1.0.0
 */

import React from 'react';

const DiagramSearch = ({tab, switchTab }) => {
    return (
        <div>
            
        </div>
    );
};

export default DiagramSearch;